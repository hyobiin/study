{
  "compilerOptions": {
    // ES5 로 변환해서 컴파일 해줌
    // ESNext: 자바스크립트 최신 버전
    "target": "ESNext",
    // 자바스크립트에서 사용되는 모듈 시스템을 설정(CommonJs, ESNext)
    "module": "ESNext"
  },
  // 컴파일 되는 대상 위치 설정
  "include": ["src"]
}
